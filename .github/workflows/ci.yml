# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Build Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]

    steps:
    - uses: actions/checkout@v2

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install Specific Chrome Version
      run: wget --no-verbose -O /tmp/chrome.deb https://dl.google.com/linux/chrome/deb/pool/main/g/google-chrome-stable/google-chrome-stable_98.0.4758.102-1_amd64.deb && apt install -y /tmp/chrome.deb && rm /tmp/chrome.deb

    - name: Report Version
      run: chrome --version

    - name: Cache multiple paths
      uses: actions/cache@v2
      with:
        path: node_modules
        key: enterprise-1020

    - name: Setup timezone
      uses: zcong1993/setup-timezone@master
      with:
        timezone: America/New_York

    - name: Install Dependencies
      run: npm i

    - name: Build
      run: npm run build

    - name: Run Lint Checks
      run: npm run lint:ci

    - name: Run functional tests
      run: npm run functional:ci

    - name: Run puppeteer tests
      run:  (npm run quickstart &) && sleep 5; npm run e2e:puppeteer;

    - name: Run e2e tests
      run:  (npm run quickstart &) && sleep 5; npm run e2e:ci:debug;

    - name: Deploy Main
      env:
        JENKINS_JOB_TOKEN: ${{ secrets.JENKINS_JOB_TOKEN }}
        JENKINS_API_TOKEN: ${{ secrets.JENKINS_API_TOKEN }}
      if: github.event_name == 'push'
      run: sh ./scripts/jenkins-deploy.sh -b main

    - name: Dump GitHub context
      env:
        GITHUB_CONTEXT: ${{ toJson(github) }}
      run: echo "$GITHUB_CONTEXT"

    - name: Dump job context
      env:
        JOB_CONTEXT: ${{ toJson(job) }}
      run: echo "$JOB_CONTEXT"
