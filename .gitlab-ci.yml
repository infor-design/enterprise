stages:
  - test
  - deploy

node_testing:
  stage: test
  image: macos-13-xcode-14
  script:
    - npm ci
    - npm run build
    - npm run lint
    - (npm run quickstart &) && sleep 5; npm run test
    - echo "$GITHUB_CONTEXT"
    - echo "$JOB_CONTEXT"
  tags:
    - dsa
    - DIND
